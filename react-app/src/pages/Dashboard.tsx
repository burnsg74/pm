import { Alert } from "react-bootstrap"

const Dashboard = () => {
    return (<>
        <h1>Dashboard</h1>
        <Alert key="alert-success" variant="success" >Alert Test </Alert>
        </>);
};

export default Dashboard;